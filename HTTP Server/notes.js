// we are creating a fertch api 

// async function getResponse() {
//     const response = await fetch('here we will put any link from where we will fetch the data')
//     const data = await response.json()
//     console.log(data)
    
// }

// What is Axios?
// Axios is a promise-based HTTP Client for node.js and the browser. 
// It is isomorphic (= it can run in the browser and node.js with the same codebase). 
// On the server-side it uses the native node.js http module, while on the client (browser) 
// it uses XMLHttpRequest.